# Linear Regression Demo

Minimal interactive walkthrough of linear regression using React and Vite.

## Requirements
- Node.js
- Gemini API key

## Run Locally
1. Install dependencies:
   `npm install`
2. Create `.env.local` and set:
   `GEMINI_API_KEY=your_key_here`
3. Start the dev server:
   `npm run dev`
